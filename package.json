{
  "name": "deepstudy-ai-landing",
  "version": "1.0.0",
  "description": "Landing page for DeepStudy.AI",
  "scripts": {
    "build": "tailwindcss -i ./styles/input.css -o ./output.css --minify",
    "watch": "tailwindcss -i ./styles/input.css -o ./output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
